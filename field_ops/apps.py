from django.apps import AppConfig


class FieldOpsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'field_ops'
    verbose_name = 'Field Ops (Phase 8 — GPS + Timeclock)'
